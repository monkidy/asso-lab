# CLAUDE.md: Asso Lab / ACE

Single source of truth for any Claude Code session on this repo.
Read this before touching anything. Update it when the system changes.

---

## What this project is

**Asso Lab** is the public observer surface of the ACE doctrine.
ACE = Autonomous execution Governance: closed-by-default, evidence-first,
human bounds before autonomy, receipts over claims.

Operator: Hichem Benali (@dismerciatonton on X).
Agent: Claude (ACE CM).

---

## Hard rules (non-negotiable)

- No em-dash ever (the character). The post_to_x.py script refuses to post if present.
- X output = English. LinkedIn output = French. Never mix.
- No links in post body (X throttles reach). Links go in the reply to the last tweet.
- Receipts are generated by code, never by the model.
- Nothing is published without explicit operator approval (telegram gate or direct confirmation).
- Never commit .env, .env.x, or any file containing credentials.
- Never write to the repo without a receipt or a clear operator instruction.

---

## Repo structure

```
briefing_orchestrator_v0.py   Daily intelligence brief (Gemini, 5 RSS sources)
telegram_gate.py              Sends draft to Hichem via Asso_CM bot, waits ok/non
post_to_x.py                  Posts to X (@dismerciatonton) with Proof of Agent receipt
run_pipeline.py               Full pipeline: orchestrator -> gate -> post (one command)
run.ps1                       Legacy local runner (interactive, Windows only)
setup_x_env.ps1               One-time X credential setup helper

docs/comms-field-note-format.md   Canonical X + LinkedIn format (ACE Field Note)
ACE-Operating-Doctrine.md         Public doctrine anchor
brand/BRAND.md                    Visual identity (ink/paper, monospace, proof seal)

publications/YYYY-MM-DD-briefing.md   Daily briefs (DRAFT until gate approves)
receipts/YYYY-MM-DD-receipt.json      Code-generated receipts (DRAFT -> PUBLISHED)
proof-of-agent/receipts/YYYY-MM-DD.json   Action receipts (x_post, x_reply, etc.)
logs/.orchestrator_log.json           Internal run log (last ping, entries)
```

---

## Credentials (never committed, local .env only)

```
GEMINI_API_KEY              Gemini API for briefing generation
TELEGRAM_BOT_TOKEN          Asso_CM bot token (from @BotFather)
TELEGRAM_CHAT_ID            Hichem's chat ID with Asso_CM
X_API_KEY                   X API key
X_API_SECRET                X API secret
X_ACCESS_TOKEN              X access token
X_ACCESS_TOKEN_SECRET       X access token secret
```

See .env.example for the full template.

---

## The publishing pipeline

### Full automated run (one command)

```bash
python run_pipeline.py
```

Flow:
1. briefing_orchestrator_v0.py generates today's brief (DRAFT, committed)
2. telegram_gate.py sends the X post draft to Asso_CM bot
3. Hichem replies "ok" to approve or "non" to refuse
4. If approved: post_to_x.py posts to X + generates Proof of Agent receipt
5. Receipt committed to proof-of-agent/receipts/

### Manual steps (fallback)

```bash
# 1. Generate brief
python briefing_orchestrator_v0.py

# 2. Draft and gate an X post
python telegram_gate.py --file drafts/x_post.txt --channel X

# 3. Post if approved
python post_to_x.py --file drafts/x_post.txt
```

### Gate keywords

Approve: ok, oui, valide, go, yes, 1, approve, post
Refuse:  non, no, refuse, annule, stop, 0, cancel, nope

---

## Comms format: ACE Field Note

Source of truth: docs/comms-field-note-format.md

### X post (EN, solo, max 280 chars)

1. Contrarian hook (stops the scroll)
2. 2-3 lines: insight through R.O.C lens
3. Proof when possible (number, fact, or receipt screenshot)
4. Final line: principle, usually a variant of "Receipts over claims."

Post solo first. Thread only once traction appears.
Series label: ACE Field Note #N (increment per published brief).

### LinkedIn post (FR)

1. Hook, same contrarian energy, in French
2. Short paragraphs, one idea each, generous white space
3. R.O.C angle as a point of view
4. Proof or concrete example
5. Close on the principle
6. No links in body (first comment only)

### One brief = four formats

X post (EN) + thread (EN) + LinkedIn (FR) + newsletter snippet.
Produce all four in one session run.

---

## Routines

### Intelligence veille (3x per weekday)

Run by Claude Code on demand. Produces a 4-flux report:
1. Leads (buying signals, job postings for AI reliability/governance)
2. Competitors (agent observability, guardrails, governance tools)
3. Hot topics (agent failures, HITL debate, EU AI Act)
4. OSS (OPA, Cedar, OTel GenAI, guardrails repos)

Output: private intel, never committed to repo.

### Daily comms-prep (weekdays, target 13h CET)

Google Calendar event set: "ACE Brief : verifier + poster" (daily, 13h00, popup 5 min, email 30 min).
Asso_CM bot sends a Telegram push when the brief DRAFT is ready.

---

## Doctrine pillars (for content and code)

- Closed by Default: nothing allowed unless explicitly admitted
- Evidence First: receipts over claims
- Human Bounds: agents propose, humans decide
- Fail-Closed: when in doubt, stop and surface the reason

Counter-position on competitors:
- Observability tools (Galileo, Arize, Langfuse): they watch what happened. ACE governs what is allowed.
- Microsoft AGT: installs the rails. ACE tells you how to drive, when to stop, how to investigate.

---

## Target accounts for X reply game

docs/comms-field-note-format.md has the full list.
Key: Simon Willison, swyx, Charity Majors, Hamel Husain, @killix.
Spend more time replying than posting. Sharp reply in first 10-30 min after a big account posts.

---

## Field Note numbering

First post = ACE Field Note #1. Increment per published brief.
No prior published Field Notes before 2026-06-05.

---

## Version

Last updated: 2026-06-05
Status: OPERATIONAL
