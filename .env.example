GEMINI_API_KEY=your_key_here

# Telegram notification (optionnel — brief pret alerting)
# 1. Cree un bot via @BotFather sur Telegram -> copie le token
# 2. Envoie un message a ton bot, puis :
#    curl https://api.telegram.org/bot<TOKEN>/getUpdates  -> copie chat.id
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
